#include <stdio.h>

typedef struct {
  int x;
  int y;
} Point;

Point pts[10];
int ints[10];

/* none of the adds could be approximated */
int main(){
  int arg1; Point p;
  arg1 = arg1 + p.x;
  if(arg1 > 0) {
    ints[5] = 3;
  } else {
    ints[2] = 1;
  }
  return 0;
}
