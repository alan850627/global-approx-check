#include <stdio.h>

typedef struct {
  int x;
  int y;
} Point;

Point pts[10];
int ints[10];

/* none of the adds could be approximated */
int main(){
  int arg1; Point p;
  if (p.x > 10) {
    arg1++;
    p.y += 2;
  }
  if (arg1 > 10) {
    if (p.y < 1) {
      ints[3] = 6;
    }
  }
  return 0;
}
